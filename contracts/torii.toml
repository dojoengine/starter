# Torii indexer config — connects to Katana's RPC to index world events into a queryable store.
rpc = "http://localhost:5050"

[server]
http_addr = "0.0.0.0"
http_port = 8080
http_cors_origins = ["*"]
